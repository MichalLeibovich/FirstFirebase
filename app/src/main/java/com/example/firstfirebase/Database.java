package com.example.firstfirebase;

import android.util.Log;

import com.google.android.gms.tasks.OnFailureListener;
import com.google.android.gms.tasks.OnSuccessListener;
import com.google.firebase.firestore.DocumentReference;
import com.google.firebase.firestore.FirebaseFirestore;

public class Database {

    private FirebaseFirestore firebaseFirestore;

    //private void enterUserToFirebase(User u) {
        //firebaseFirebase = FirebaseFirestore.getInstance();
        //       firebaseFirebase.collection("users").add(u).addSuccessListener(new OnSuccessListener<>(){
//            @Override
//            public void onSuccess(DocumentReference DocumentReference) {
//                Log.d("FIREBASE ADD", "onSuccess: " + Sustem.currentTimeMillis());
//            }
//        });
//
//        .addOnFailureListener(new OnFailureListener()){
//            @override
//                    public void onFailure(@NonNull Exception e) {
//                Log.d("FIREBASE ADD", "onFail: " + System.currentTimeMillis());
//        }
//    });
//Log.d("FIREBASE ADD", "enterUserToFirebase: after " + System.currentTimeMillis());
//    }
    }
